# NETWRAITH dashboard talks to the bridge over these two endpoints.
# Copy this file to .env.local and adjust if the bridge runs elsewhere.
NEXT_PUBLIC_BRIDGE_URL=http://localhost:4317
NEXT_PUBLIC_BRIDGE_WS=ws://localhost:4317
